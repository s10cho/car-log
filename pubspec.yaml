name: car_log
description: "개인 차량의 유지보수 기록을 빠르게 남기고 확인하는 앱."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.3

dependencies:
  drift: ^2.31.0
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_riverpod: ^3.1.0
  flutter_secure_storage: ^11.1.0
  go_router: ^18.0.1
  intl: ^0.20.3
  logging: ^1.3.0
  meta: ^1.19.0
  path: ^1.9.1
  path_provider: ^2.1.6
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.1
  drift_dev: ^2.31.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
